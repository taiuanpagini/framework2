<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "Framework Padrao" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Framework Padrao" %>">
    <link rel="icon" type="image/png" href="img/favicon.png" sizes="32x32">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Roboto:400,900,500,700' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
